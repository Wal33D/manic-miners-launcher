{
  "loader": "ts-node/esm",
  "extensions": ["ts"],
  "spec": "src/tests/**/*.test.ts",
  "exclude": ["src/tests/realDownloadIntegration.test.ts"],
  "timeout": 10000,
  "reporter": "spec",
  "recursive": true,
  "require": ["ts-node/register"]
}
